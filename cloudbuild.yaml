steps:
  # [START copy-dags]
  - id: 'copy-dags'
    name: 'google/cloud-sdk'
    dir: './infra'
    entrypoint: 'bash'
    args:
      - '-c'
      - |
          if [ "${BRANCH_NAME}" == "main" ] && [ "${_TAG}" == "dev" ] ; then
            echo "deploy"
          else
            echo "don't deploy - ${BRANCH_NAME} - ${_TAG}"
          fi
  # [START copy-dags]
